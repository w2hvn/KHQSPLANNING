<UserControl x:Class="MilitaryTrainingApp.Views.SystemConfig.TargetRuleConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ĐỊNH MỨC THỜI GIAN THEO ĐỐI TƯỢNG" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,10"/>

        <DataGrid x:Name="dgTargets" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" BorderBrush="{StaticResource BorderBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Đối tượng" Binding="{Binding TargetCode}" Width="100" IsReadOnly="True" Foreground="Gray"/>
                <DataGridTextColumn Header="Tên Đối tượng" Binding="{Binding TargetName}" Width="*" IsReadOnly="True"/>

                <DataGridTextColumn Header="Ngày HL/Tuần" Binding="{Binding DaysPerWeek}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Giờ Sáng (h)" Binding="{Binding MorningHours, StringFormat={}{0:0.#}}" Width="90">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Center"/></Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Giờ Chiều (h)" Binding="{Binding AfternoonHours, StringFormat={}{0:0.#}}" Width="90">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Center"/></Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Giờ Tối (h)" Binding="{Binding NightHours, StringFormat={}{0:0.#}}" Width="90">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Center"/></Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Tổng Giờ/Tuần" Binding="{Binding MaxHoursPerWeek, StringFormat={}{0:0.#}}" Width="120" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="DarkRed"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Tải Lại" Width="100" Margin="0,0,10,0" Background="Gray" Click="BtnRefresh_Click"/>
            <Button Content="Lưu Định Mức" Width="130" Background="{StaticResource AccentBrush}" FontWeight="Bold" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
