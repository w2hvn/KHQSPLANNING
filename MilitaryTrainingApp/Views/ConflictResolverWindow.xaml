<Window x:Class="MilitaryTrainingApp.Views.ConflictResolverWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trợ lý Chỉ huy - Xử lý phân vân" Height="300" Width="600" WindowStartupLocation="CenterOwner" Topmost="True" Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtTitle" Text="Phát hiện phân vân tại Môn: ---" FontSize="16" FontWeight="Bold" Foreground="DarkRed" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <ProgressBar x:Name="pbProgress" Height="15" Margin="0,0,0,15" Value="0" Maximum="100"/>

        <TextBlock Grid.Row="2" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,20"
                   Text="Hệ thống phát hiện mốc thời gian có trọng số ngang bằng nhau. Vui lòng chọn phương án phân bổ:"/>

        <StackPanel Grid.Row="3" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Phương án 1: Tập trung 100% vào Nửa đầu (GĐ1 / Tháng đầu)" Background="{StaticResource AccentBrush}" Foreground="White" FontWeight="Bold" Padding="15,10" Margin="0,0,0,10" Click="BtnOption1_Click"/>
            <Button Content="Phương án 2: Dải đều 50% Nửa đầu - 50% Nửa sau" Background="{StaticResource PrimaryBrush}" Foreground="White" FontWeight="Bold" Padding="15,10" Margin="0,0,0,10" Click="BtnOption2_Click"/>
            <Button Content="Phương án 3: Cán bộ tự nhập (Bỏ qua)" Background="Gray" Foreground="White" FontWeight="Bold" Padding="15,10" Click="BtnOption3_Click"/>
        </StackPanel>
    </Grid>
</Window>
