<Page x:Class="MilitaryTrainingApp.Views.TrainingTargetPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Danh mục Đối tượng Huấn luyện" Background="{StaticResource BackgroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="DANH MỤC ĐỐI TƯỢNG HUẤN LUYỆN CORE" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
            <Button Content="Thêm Đối Tượng Mới" Margin="20,0,0,0" Padding="10,5" Click="BtnAdd_Click"/>
            <Button Content="Xóa Dòng Chọn" Margin="5,0,0,0" Padding="10,5" Background="DarkRed" Foreground="White" Click="BtnDelete_Click"/>
            <Button Content="Lưu Cập Nhật" Margin="5,0,0,0" Padding="10,5" Background="{StaticResource AccentBrush}" Foreground="White" Click="BtnSave_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgTargets" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Đối Tượng (Code)" Binding="{Binding Code}" Width="150"/>
                <DataGridTextColumn Header="Tên Đối Tượng (Name)" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Thứ Tự Sắp Xếp (Sort Order)" Binding="{Binding SortOrder}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
