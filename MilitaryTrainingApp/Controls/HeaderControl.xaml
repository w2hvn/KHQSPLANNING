<UserControl x:Class="MilitaryTrainingApp.Controls.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource PrimaryBrush}">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Horizontal">
            <TextBlock Text="KẾ HOẠCH:" Foreground="White" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,5,0"/>
            <ComboBox x:Name="cboPlan" Width="200" DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="CboPlan_SelectionChanged"/>
        </StackPanel>

        <StackPanel Grid.Column="2" Orientation="Horizontal">
            <TextBlock Text="ĐỐI TƯỢNG:" Foreground="White" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,5,0"/>
            <ComboBox x:Name="cboTarget" Width="200" DisplayMemberPath="DisplayText" SelectedValuePath="PlanTargetId" SelectionChanged="CboTarget_SelectionChanged"/>
        </StackPanel>

        <StackPanel Grid.Column="4" Orientation="Horizontal">
            <TextBlock Text="NGỮ CẢNH T.GIAN:" Foreground="White" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,5,0"/>
            <ComboBox x:Name="cboActiveTimeNode" Width="350" DisplayMemberPath="DisplayText" SelectedValuePath="TimeNodeId" SelectionChanged="CboActiveTimeNode_SelectionChanged" FontFamily="Consolas, Courier New"/>
        </StackPanel>
    </Grid>
</UserControl>
